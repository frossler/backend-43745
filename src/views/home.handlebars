<ul>
{{#if products.length}}
    {{#each products}}
        <li> {{title}} </li>
        <li>Description: {{description}} </li>
        <li>Price: {{price}} </li>
        <li>Stock: {{stock}} </li>
        <br>
    {{/each}}
{{else}}
    <p>No products to show.</p>
{{/if}}
</ul>